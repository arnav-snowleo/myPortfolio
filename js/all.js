AOS.init({
    duration: 3000,
  })

//typing animation

var options = {
  strings: ["Projects ^1000","Apps ^1000" , "Websites ^1000"],
  typeSpeed: 100,
  backSpeed: 60,
 
  loop: true,
};

var typed = new Typed(".metyping",options);